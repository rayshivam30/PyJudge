# PyJudge Frontend

The SRS calls for a React frontend. This workspace currently contains the backend implementation scaffold first, because the execution and data model are the highest-risk parts of the platform.

Recommended next step: scaffold a Vite React app here and connect it to the FastAPI endpoints exposed at `/docs`.
